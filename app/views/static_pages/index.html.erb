<% if params[:user_id].nil? %>
      <%= render 'form' %>
<% else %>
    <% @images.each do |photo| %>
      <img src=<%=Flickr.url_n(photo)%>>
    <% end %>
    <br />
    <%= link_to "Choose a new user", root_path, class: "button is-info" %>
<% end %>


